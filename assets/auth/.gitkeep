# Optional: replace auth_cinematic.mp4 with a richer H.264 montage
# (720p/1080p, muted-friendly, ~3–6 MB). AuthCinematicBackground auto-plays
# this file when present; otherwise falls back to the Ken Burns collage.
