<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- ~22dp inset on the 108dp adaptive canvas so the mark isn’t oversized
       under circle / squircle / rounded-rect masks (API 26+). -->
  <item
    android:drawable="@drawable/ic_launcher_foreground"
    android:left="22dp"
    android:top="22dp"
    android:right="22dp"
    android:bottom="22dp" />
</layer-list>
