# Copy to .env.qa.local (gitignored). Used by mobile_feed_playback_matrix.sh
# and android_bad_index_impeller_ab.sh when env vars are not exported.
QA_EMAIL_OR_USERNAME=tester
QA_PASSWORD=StreamersTip@1

# Device E2E (also pass --dart-define=RUN_MOBILE_FEED_E2E=true):
#   source scripts/_load_qa_credentials.sh && _load_qa_credentials "$PWD"
#   flutter test integration_test/home_feed_playback_e2e_test.dart -d <device> \
#     --dart-define=RUN_MOBILE_FEED_E2E=true \
#     --dart-define=QA_EMAIL_OR_USERNAME="${QA_EMAIL_OR_USERNAME}" \
#     --dart-define=QA_PASSWORD="${QA_PASSWORD}"
